<!DOCTYPE html>
<html lang="en">
    <head>
        <link rel="stylesheet" type="text/css" href="{{ styles_path }}">
        <link rel="icon" type="image/x-icon" href="{{ favicon_path}}">
        <title>{{ page_title }}</title>
    </head>
    <body>
        <div class="header">
            <a class="logo center-flex" href="{{ logo_link_path }}"><img class="logo" src="{{ logo_path }}" 
                alt="Multnomah County Library Logo"></a>
            <div class="header-right center-flex">
                {% for menu_item in menu_items %}
                    &nbsp;&nbsp;<a class="{{ menu_item.class }}" href="{{ base_URL }}{{ menu_item.link }}">{{ menu_item.name }}</a>
                {% endfor %}
            </div>
        </div>
